// calculate distance between two points

class Solution {
    public int distance(int x1, int y1, int x2, int y2) {
      
        int res = (int)Math.round(Math.sqrt(Math.pow(x2-x1,2) + Math.pow(y2-y1,2)));
        return res;
    
    }
}
